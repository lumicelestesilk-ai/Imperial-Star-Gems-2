node_modules/
.next/
out/
build/
.DS_Store
*.pem
.env
.env*.local
next-env.d.ts
*.tsbuildinfo

# Raw PNG source frames are an intermediate build input, never shipped.
# Keep them out of the repo; `npm run sequence:convert` regenerates public/sequence.
assets/sequence/raw/
assets/sequence/raw-700/
assets/sequence/raw-360/
assets/sequence/wire-700/
assets/sequence/wire-360/

# Flag SVGs are copied out of the flag-icons package by scripts/copy-flags.mjs
# (run automatically by `npm run build`). Regenerate rather than commit them.
public/flags/
